# Frontend (Ishpreet)

The web UI goes here. The backend API it talks to is documented in
../backend/README.md - base url is http://localhost:5000 (or whatever
port the server prints when it starts).

For the real-time watchlist use the SSE stream endpoint, example
javascript is in the backend README.
